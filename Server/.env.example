# Colyseus Server Configuration
# NODE_ENV: Environment mode (development, production)
NODE_ENV=production

# PORT: HTTP port for the server (must match Akash Network mapping)
PORT=2567

# WS_PORT: WebSocket port for client connections
WS_PORT=2567

# Security Configuration
# JWT_SECRET: Secret key for JWT token signing (must be strong and unique)
JWT_SECRET=your-very-secure-jwt-secret-here

# AUTH_SALT: Salt for password hashing
# AUTH_SALT=your-secure-auth-salt-here

# SESSION_SECRET: Secret for session encryption
# SESSION_SECRET=your-secure-session-secret-here

# Colyseus Monitor
# COLYSEUS_MONITOR_PASSWORD: Password for accessing the Colyseus monitor dashboard
COLYSEUS_MONITOR_PASSWORD=admin-password-here

# Logging
# LOG_LEVEL: Logging level (error, warn, info, verbose, debug, silly)
LOG_LEVEL=info

# Akash Network Specific
# COLYSEUS_SERVER_NAME: Name of the server instance
COLYSEUS_SERVER_NAME=colyseus-unity-server

# COLYSEUS_SERVER_REGION: Deployment region
COLYSEUS_SERVER_REGION=us-central
